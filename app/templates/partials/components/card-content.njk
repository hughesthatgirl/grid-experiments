<div class="card--content">
    <div class="card--title">{{ item.card_title }}</div>
    <p class="card--snippet">{{ item.card_content }}</p>
    <a class="card--button" href="{{ item.card_link_url }}">{{ item.card_button }}</a>
</div>