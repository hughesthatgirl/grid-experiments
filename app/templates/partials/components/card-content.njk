<div class="card--content background--{{ item.card_bg_color }}">
    <div class="card--title">{{ item.editable_BCC_card_title }}</div>
    <p class="card--snippet">{{ item.editable_BCC_card_content }}</p>
    <a class="card--button" href="{{ item.editable_BCC_card_link_url }}">{{ item.editable_BCC_card_button }}</a>
</div>