{% extends "partials/layouts/layout-container.njk" %}
{% block content %}
    <div class="layout-row max-width-{{ row_width }} {{ full_width_class }}">
        <h{{editable_BCC_heading_type}}>{{ editable_BCC_row_heading }}</h{{editable_BCC_heading_type}}>
        {% for paragraph in paragraphs %}
            <p>{{ paragraph.editable_BCC_text }}</p>
        {% endfor %}
    </div>
{% endblock %}