{% extends "partials/layouts/layout-two-column.njk" %}
{% block item %}
    {% if item.heading %}
        <h3>{{ item.heading }}</h3>
    {% endif %}
    {% if item.content %}
        <p>{{ item.content }}</p>
    {% endif %}
{% endblock %}